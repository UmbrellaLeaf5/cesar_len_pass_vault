1. Добавить основное шифрование, использующее crypt функции из cesar_len_key (а текущую реализацию сделать резервной и хранить в отдельном файле на яндекс хранилище)
2. Разобраться с дизайном:
   - пусть теперь при нажатии настроек (кнопки "\*") будет выпадать список из одного элемента "загрузить резервную", при нажатии на этот пункт как раз и будет скачиваться шифрованная реализация, использующая текущий вариант, а не оригинальный cesar_len_key
   - убрать бесполезную кнопку копировать (так как весь текст целиком тут не нужен, требуются только выборочные пароли)
   - сделать центрирование текста относительно вертикали на разблокировочном экране
3. Завести enum для state, не использовать строки
4. Проверить, что в разделе notes сохраняется многострочность
5. Создать релизы для мобильной и декстопной версий
